# Ruff configuration for AlterScore

# Target Python version
target-version = "py312"

[lint]
# Add rules to ignore
ignore = []

[lint.per-file-ignores]
# Ignore E402 (import not at top of file) in tests because of sys.path.insert.
"tests/**/*.py" = ["E402"]
