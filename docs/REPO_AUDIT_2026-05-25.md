# Repository Audit - May 25, 2026

## Scope

This audit covered repository hygiene, ignored/generated files, manifest/runtime
alignment, frontend/backend documentation drift, and the current roadmap/TODO
state.

## Current Source Of Truth

- Git branch: `main`
- Git tracked state before edits: clean
- Active runtime manifest: `models/registry/production_manifest.json`
- Active runtime model: `xgboost_monotonic`
- Manifest version: `xgboost_monotonic_v1`
- Runtime type: `classical_monotonic`
- Checked-in test AUC: `0.8040`
- Checked-in Brier score: `0.1514`
- Checked-in ECE: `0.0284`
- Checked-in PSI verdict: `stable`
- Fairness status: attention required for `gender=non_binary`

## Cleanup Performed

Removed ignored generated byproducts from the workspace:

- Python `__pycache__` directories under `backend/` and `tests/`
- `.runtime/`
- `frontend/dist/`
- generated runtime reports under `runtime/governed_reports/` and
  `runtime/research_archive/`
- local runtime log files while preserving `runtime/logs/.gitkeep`
- generated synthetic data and validation outputs under `data/raw/` and
  `data/validation/`
- restricted `runtime/pytest-temp/`

Left in place:

- `venv/`
- `frontend/node_modules/`

Those are local dependency installs, ignored by Git, and useful for validation.

## Mismatches Fixed

- Updated repo docs that still described the calibrated stacking ensemble as
  the active runtime.
- Documented the monotonic `XGBoost` runtime as the checked-in manifest source
  of truth.
- Preserved the calibrated stacking ensemble as benchmark and
  rollback/reference infrastructure, not the default runtime.
- Updated roadmap and TODOs to reflect dashboard partial wiring,
  release/demo runbook needs, and the fairness/report reconciliation work.
- Added a PRD implementation addendum that explains why the implemented runtime
  differs from the original stacking-ensemble target.
- Fixed the dashboard metric card so it prefers the active manifest model
  instead of the first test metric row.
- Removed stale dashboard fallback values that could show fabricated metrics
  when data is missing.
- Expanded `.gitignore` for common Python, coverage, and frontend byproducts.

## Remaining Risks

- The governed full-run review reports stronger monotonic `XGBoost` metrics
  than the checked-in promoted artifact reports. This needs reconciliation
  before pilot claims.
- The checked-in fairness report requires attention for `gender=non_binary`.
- The manifest still records historical `code_ref: "antigravity/dev"` instead
  of a precise branch/commit identifier.
- Dashboard still needs confusion-matrix rendering and per-panel error states.
- Image-based packaging has been removed from scope per project direction.

## Recommended Roadmap

1. Reconcile promoted runtime metrics, fairness status, and manifest metadata.
2. Harden or formally accept the `gender=non_binary` fairness finding.
3. Run browser QA for borrower and dashboard flows on mobile and desktop.
4. Add frontend tests for score payloads, retry behavior, results rendering,
   dashboard data states, and endpoint failure behavior.
5. Render `/api/confusion-matrix` in the evaluator dashboard.
6. Write and validate a local/manual release runbook.
7. Finalize release smoke checklist, demo script, and rollback notes.

## Validation Targets

- `python -m pytest tests/integration/api/test_checked_in_runtime_bundle_smoke.py`
- `python -m pytest tests/integration/api/test_score_endpoint.py tests/integration/api/test_analytics_endpoints.py`
- `npm run build` from `frontend/`
- Manual backend/frontend startup smoke once Track G resumes
